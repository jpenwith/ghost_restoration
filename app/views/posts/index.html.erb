<h1>Posts</h1>

<ul>
    <% @posts.each do |post| %>
        <li>
            <a href="/posts/<%= post.uuid %>?index_last_offset=<%= @offset %>">
                <%= post.published_at %>
                <% if !post.restored_post.nil? %>✅<% end %>
            </a>
        </li>
    <% end %>
</ul>

<a href="posts?offset=<%= @offset + @posts.count %>">
    Next &gt;
</a>